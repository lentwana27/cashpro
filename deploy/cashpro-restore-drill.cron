# Monthly restore drill for cashpro: proves the latest backup actually restores
# and matches live row counts. Runs on the 1st, after the daily 02:30 backup.
30 3 1 * * root /root/restore-drill-cashpro.sh >> /var/log/cashpro-restore-drill.log 2>&1
